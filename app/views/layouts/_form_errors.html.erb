<% if the_thing.errors.any? %>
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= pluralize(the_thing.errors.count, "error") %> prohibited this brand from being saved:
      </div>
      <div class="panel-body">
        <ul>
          <% the_thing.errors.full_messages.each do |message| %>
              <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
<% end %>
